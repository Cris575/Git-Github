### ⚙️ Configuración global de Git

```bash
git config --global user.name "Cris575"
git config --global user.email "test@email.com"
```

> 💡 Nota:
> `--global` aplica la configuración a todos tus repositorios en tu equipo.
